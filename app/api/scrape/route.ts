import { NextResponse } from "next/server";

// Placeholder — implemented in M7 with Supabase Edge Function
export async function GET() {
  return NextResponse.json({ message: "Scraper scheduled via Supabase Edge Functions" });
}
